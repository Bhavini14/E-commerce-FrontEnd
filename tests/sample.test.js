import { describe, it, expect } from 'vitest'

describe('Basic Test', () => {
  it('adds numbers correctly', () => {
    expect(2 + 2).toBe(4)
  })
})


describe('Basic Test', () => {
  it('Subtractions numbers correctly', () => {
    expect(2 - 2).toBe(0)
  })
})